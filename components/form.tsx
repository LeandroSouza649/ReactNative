import React from "react";
import { Button, Text, TextInput, View,} from "react-native";

interface Props {
    title: string;
    onPress: () => void
}
export default function Form({title, onPress}: Props){
    
}

